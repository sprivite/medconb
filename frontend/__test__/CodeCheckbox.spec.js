describe('CodeCheckbox specs', () => {
  test.todo('Changed implementation')
  // afterEach(() => {
  //   jest.clearAllMocks()
  // })
  // test('Shows tooltip after 2 seconds', () => {
  //   const wrapper = shallow(<CodeCheckbox label="test" />)

  //   const root = wrapper.find('Root')
  //   root.simulate('mouseenter')

  //   expect(setTimeout).toHaveBeenCalledTimes(1)
  //   expect(setTimeout).toHaveBeenLastCalledWith(expect.any(Function), 2000)

  //   jest.advanceTimersByTime(2000)
  //   expect(wrapper.find('Tooltip').prop('visible')).toBe(true)
  // })

  // test('cancels timeout on mouseleave', () => {
  //   const wrapper = shallow(<CodeCheckbox label="test" />)

  //   const root = wrapper.find('Root')
  //   root.simulate('mouseenter')

  //   expect(setTimeout).toHaveBeenCalledTimes(1)
  //   expect(setTimeout).toHaveBeenLastCalledWith(expect.any(Function), 2000)

  //   root.simulate('mouseleave')
  //   expect(clearTimeout).toHaveBeenCalledTimes(1)

  //   jest.advanceTimersByTime(2000)
  //   expect(wrapper.find('Tooltip').prop('visible')).toBe(false)
  // })
})
