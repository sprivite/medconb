import React from 'react'
import CodeCheckbox from '../src/components/CodeCheckbox'

export default {
  component: CodeCheckbox,
  title: 'CodeCheckbox',
}

const Template = (args: any) => <CodeCheckbox {...args} />

export const Default = Template.bind({})
