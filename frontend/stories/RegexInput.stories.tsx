import React from 'react'
import RegexInput from '../src/components/RegexInput'

export default {
  component: RegexInput,
  title: 'RegexInput',
}

const Template = (args: any) => <RegexInput {...args} />

export const Default = Template.bind({})
